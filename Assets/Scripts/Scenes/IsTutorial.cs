using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IsTutorial : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        gameStatistics.isTutorial = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
